# Generation feature boundary

This feature turns one natural-language request into a reviewable Agent or Team
Draft. It never writes a business record and never bypasses existing permission,
model, Skill, MCP, Agent, or Team validation.
